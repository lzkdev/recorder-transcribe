# Contributing

修改脚本后运行 `python3 -m unittest discover -s tests -v`。新增 CLI 适配请附官方命令版本、脱敏响应结构和失败行为测试。

测试文件在运行时生成。不要提交真实录音、逐字稿、妙记链接、用户身份、token 或 `.env`。不要把网络测试称为离线测试，也不要将模拟响应写成真实端到端验证结果。

优先维护不重复提交、源文件不变和结果可恢复三个不变量。远程 MCP 接入是独立贡献方向，需要认证、租户隔离和文件上传设计，不能直接暴露 shell。
